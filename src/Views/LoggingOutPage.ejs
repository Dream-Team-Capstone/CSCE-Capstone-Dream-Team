<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/Scripts/settingsHandler.js" defer></script>
    <!-- Hidden input to indicate if user is logged in -->
    <input type="hidden" name="isLoggedIn" value="<%= locals.user ? 'true' : 'false' %>">
    <title>Logging You Out</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            padding: 50px;
        }
        .loading-spinner {
            border: 8px solid #f3f3f3 !important; /* Light grey */
            border-top: 8px solid #3498db !important; /* Blue */
            border-radius: 50% !important;
            width: 50px !important;
            height: 50px !important;
            animation: spin 2s linear infinite !important;
            margin: 20px auto !important;
            z-index: 1 !important;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .message {
            font-size: 1.2rem;
            margin-top: 20px;
        }
    </style>
    <link rel="stylesheet" href="/StylesPages/accessibilityStyles.css">
</head>
<body>
    <h1>Logging you out...</h1>
    <div class="loading-spinner"></div>
    <p class="message">You will be redirected to the homepage when successfully logged out.</p>

    <script>
        setTimeout(() => {
            window.location.href = '/api/home'; // Redirect after 2 seconds
        }, 3000); // 3 secs
    </script>

</body>
</html>
